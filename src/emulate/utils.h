#include <stdint.h>

// This prints out the bits of a number
extern void printBits(uint64_t, int);

// This returns the size of a given file in bytes
extern int sizeOfFile(char*);

extern uint64_t power(uint64_t, uint64_t);
